* Добавлено переключение варианта из нативного селекта
* Добавлено переключение варианта из урла
* Добавлен класс Products для создания новых продуктов
* Добавлен rigger в gulp для склейки js
* к ProductVariants добавлено свойство images, которое доступно при рендере опшнселектора
* добавлена валидация настроек продукта в тулзы, чтоб методы были доступны во всех объектах которые работают с продуктом
* Добавлен массив не фильтрованных по вариантам опций - options, но у каждой опции есть свойство disabled, если в выбранном варианте опция отсутствует то у options disabled: true
//  в данном случае не задействованные опции в варианте скрываются
_.forEach(values, function (value)
//  в данном случае не задействованные опции в варианте выводятся но имеют disabled: true
_.forEach(options, function (value)
* Добавлен тул для транслита
* Добавлен дефолтный шаблон для вывода опций в виде селекта - _templateList['option-default']
* вынес бинд селекторов в /init/after.js

=== Настройки продукта ===

//  false отключает вывод optionSelector
show_variants: true

//  Данное свойство доступно в выводе опций, его можно комбинировать с option.selected == value.position & initOption. в таком случае визуально не будет выбранных опций.
сделано по просьбе сделать невыбранными опции при загрузке.
при этом в нативном селекторе первый option следует сделать пустым

initOption: true

//  Задаём шаблоны для вывода опций
options: {
  'Цвет': 'option-image'
}

//  Объект с картинками из файлов
fileUrl: {
  "красный": "http//"
}

====== Пример вызова ======
var ProductsConfig = {
  showVariants: true,
  fileUrl: fileUrl,
  initOption: true,
  options: {
    'Цвет': 'option-images',
    'Размер': 'option-span',
    'Материал': 'option-span',
    'Жесткий диск': 'option-span'
  }
}
var Products = new ISnew.Products(ProductsConfig);